{
  "name": "cpl-site",
  "version": "1.0.0",
  "description": "Clinical Performance Lab — static site + serverless email backend",
  "private": true,
  "scripts": {
    "build": "python3 build.py",
    "dev:serve": "python3 -m http.server 8000 --directory public",
    "test:tokens": "node scripts/test-tokens.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "resend": "^3.2.0"
  }
}
